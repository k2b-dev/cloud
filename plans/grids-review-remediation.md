# Grids review remediation

Epic: `aavstrsx`. Sources: `reviews/grids-review.md` and `reviews/grids-review-tests.md`.

Status: remediation verified. Every review item has a disposition below. Existing staged changes are the reviewed baseline, not new remediation. No production operations or commits are part of this plan.

## Owners and dispositions

- `01kqlupr`: B2/B3/B7/B8/B9/B14; Q1/Q6/Q10/R15. Centralize frozen-value authorization, preserve types and historical truth, and enforce credential write scope. Never reconstruct missing historical values from current data. Determine the safe old-capture contract from stored evidence.
- `63ahmn67`: B1/B13; S1; R10/R11/R12; Q2/Q4/Q7/Q11. Reserve financial identities only with committed issuance, lock/recheck sources there, verify recovery and actor/lease boundaries. Assess existing orphan claims separately; never release claims belonging to issued documents.
- `p73tn7am`: B5/B6/B16; R1/R2/R8/R14; Q8/Q9; S2/S3/S10. Stabilize semantic hashes, parameters, list schema evolution and budgets. Preserve verification of already stored integrity hashes. Optimize only demonstrated repeated work; do not introduce speculative query machinery.
- `iceol7wj`: B10/B11/B12/B15; R3/R5/R9; Q3/Q5; S5/S7/S8/S14. Align primary-artifact APIs, evidence, confirmation UI and exact-money presentation. Never silently omit non-PDF files from mixed downloads. External format interoperability is distinct from internal validation and is not certified by this work.
- `m47ecrz9`: B4/B17/B18; R4/R6/R7/R13; T1–T10; S4/S6/S9/S11/S12/S13/S15. Synchronize binder/help/CLI/skills and verification evidence. Do not fabricate dry-run output, duplicate general documentation, or perform cosmetic-only refactors.

All listed risks/questions are investigation items, not automatically accepted bugs. Record source-backed decisions and verification below as each ticket is completed. B4 is a documentation-contract defect, not a data-integrity P1. T1/acceptance 20 already has a real stale-manifest kernel regression in `packages/cloud/src/workflows/store/worker.integration.test.ts`; the review's global absence claim is incorrect, while Grids-specific coverage still needs assessment.

## Verification contract

Start with focused regressions on an isolated local database. Then run affected package types, formatting and the complete Grids verifier after the fixes form a coherent whole. Inspect tests for meaningful failure seams rather than merely counting passes. Browser checks are reserved for interactions that code-level verification cannot establish. No bank upload, payment submission, production mutation or external certification is implied.

## Progress

### Final verification

All eight phases passed without failures or skips: 2,918 tests and 21,029 assertions. Reports: `/var/folders/pj/92xppstn3r1b739_l8dcl0_40000gn/T/grids-verification-paVApM`; aggregate log: `/tmp/grids-remediation-j2sL6t/full-final-review.log`. Breakdown: standard/DB 2,833; outbox 9; Sync 6; evidence 6; browser bundle 1; real PDF 1; recovery/cleanup 2; DOM 60. Raw Grids TypeScript, Biome on 115 changed tracked files plus four new files, and `git diff --check` pass. The source was unchanged throughout this final run. Test reports use a disposable local database, not application or production data. The tests do not establish external bank/DATEV/ADDISON acceptance or a deployed visual review. No commits, deployment, live data rewrite or financial operation was performed.

### Closure map

| Review items | Disposition |
| --- | --- |
| B1, B13, S1 | Final-transaction claims and source recheck; bounded recovery of old effect-free terminal claims; issued evidence never released. |
| B2, B7, B8, B14 | Historical transitive authorization, captured relation labels, percent parity, and credential write gates repaired. |
| B3, B9, Q1 | Missing historical values cannot be truthfully reconstructed. Reads diagnose absence; calculations requiring them fail closed. No invented backfill. |
| B4, B17, B18, R6, T1–T6 | Binder/help contract aligned; acceptance strengthened with actual kernel, transaction rollback, replay, identity, empty-batch and Unicode tests. |
| B5, B6, B16, R1, R2, R8, R14, Q8 | Semantic/versioned hashes, list schema evolution, select parameters, shared bounds, per-run capture budget and safe alpha-reset refusal. Stored hash evidence retained. |
| B10–B12, B15, R5, R9, S7, S14 | Live confirmation refresh, localized dependency/error labels, generic primary artifacts/download headers, profile evidence and exact preview amounts. Mixed PDF export rejects rather than omits. |
| R3, Q2, Q3 | Non-mutating bank-character/date warnings; no external format certification or target DATEV/ADDISON import claim. |
| R4, R13, S11–S13, S15 | Help headroom recovered without dropping knowledge; Skill routes to canonical Help; CLI and source/format/decimal descriptions aligned. |
| R10–R12, Q4, Q5, Q11 | Initiator credential pin retained with clear errors; read-only preview; explicit confirm/cancel lifecycle; delegated preview is intentional; anonymous action guard retained; coalesced heartbeat and transaction fence verified. |
| Q6, Q7, Q9, Q10, R15 | Invalid list diagnostics are separate from typed values; empty financial source is explicit; safe integer number/string behavior preserved and tested; field deletion dependency and conservative four-eyes schema semantics retained. |
| S2, S5 | Batched parameter identity reads; shared DatePicker/DescriptionList/disclosure primitives. Existing modal pager stays local because public Pagination is URL-based. |
| S3, S6, S9, S10 | No speculative caching, trivial-map abstraction, import-only churn or unmeasured SQL rewrite. A separately demonstrated Combined-query rescan was optimized and regression-tested. |
| S4, S8 | No invented dry-run counts/hashes; retain the supported workflow-only document provenance variant instead of imposing an invalid reciprocal FK constraint. |
| R7, T7–T10 | Disposable local verification database and explicit no-skip phase runner are the supported acceptance path. Preserve immutable fixture evidence, SQL execution budgets and meaningful assertions. |

The detailed checkpoints below are chronological investigation evidence, including failures later corrected; this map does not turn external interoperability or missing historical data into verified behavior.

- Recovery/read completion checkpoint: real kernel lease takeover during rendering returns lost with no document, then reuses the same capture on recovery and issues exactly once. A Grids-bound stale manifest now explicitly produces WORKFLOW_MODULE_MISMATCH before any step, capture, receipt or document. Old pre-issuance claims are reclaimed on demand only for the requested identities of unissued failed/canceled runs; the existing deletion trigger rechecks the receipt/run under locks, and a later conflict rolls reclamation back. Issued-document claims survive even a later failed run. Claims/financial tests passed 16 tests/137 assertions before the strengthened issued-terminal assertion, which passed in the following financial suite. XML now has a positive Unicode/emoji roundtrip. Normal record reads show fieldErrors for absent historical captures rather than silently empty cells; typed captured null remains distinct. All call sites, including relation targets, pass field definitions. Initial focused failures identified the missing relation-target argument and two untyped SQL test assertions; both were corrected. The final kernel/finalization rerun and raw Grids tsc passed. A fresh complete eight-phase verifier is running against this fixed source in /tmp/grids-remediation-j2sL6t/full-final-review.log; do not infer acceptance before it finishes.

- Q11 disposition: the coordinator coalesces renewals into one pending promise; a timer does not create an unbounded renewal queue. The transactional heartbeat fences the workflow run row before durable effects, and the lease-takeover regression verifies the loser cannot issue. Rendering and normalization for final issuance occur before that transaction; source checks and claim allocation remain bounded by the existing 10,000-row/5-MiB contracts. No arbitrary additional timeout or independent lease mechanism is introduced. Q3 remains an explicit interoperability boundary, not a verified target-import claim: internal tests cannot establish a particular DATEV/ADDISON installation accepting UTF-8 BOM. T7/T8/T10 retain the disposable-DB verifier contract instead of deleting immutable evidence or allowing acceptance with skipped DB tests. T9 retains the integration phase timeout; actual GQL SQL still has its separate 5-second execution budget, and the whole-run performance failure was fixed at the query plan rather than increasing it.

- S5 addressed at existing UI owners: financial header dates use the shared DatePicker and inherited locale, with no ISO syntax burden in labels; the DOM test selects a calendar date and verifies it reaches the generated workflow. Query starter disclosures use collapsible DetailPanel.Section. Financial preview metadata uses DescriptionList, retaining exact totals and masked bank data. All three affected DOM regressions pass (56 assertions). The remaining compact in-modal pager intentionally stays as Buttons: the current shared Pagination requires baseUrl and link navigation, so adapting it would introduce fake URLs or unrelated shared API work. No server or permissions contract changed.

- S4 disposition: do not emit fabricated rowCount, sha256 or capturedAt in a dry-run. The published CLI contract explicitly says dry-runs validate without capturing and do not predict counts; the runtime produces a planned reference, while the binder describes actual execution output. Existing real workflow tests cover planned references feeding generation without rendering/persisting. A data-dependent dry-run cannot promise execution results; placeholder counts would falsely select branches. S8 disposition: retain the existing source constraint. The storage contract and document-browse integration intentionally support retained workflow-only documents with their own render/template snapshot and no query_data_id; that is distinct from a query-backed document. Query-backed writes remain bound by the composite capture/run FK and issuance owner. A reciprocal constraint would reject that existing supported provenance variant rather than merely repair the query path. No stored document was rewritten or detached.

- Combined export performance: EXPLAIN ANALYZE of the actual bounded first-page SQL showed source rows rescanned for unrelated target-table candidates (10,026 rows, two loops even in a two-table fixture). Binding the already-known plan.tableId explicitly on the existing tables join removes that rescan without removing publication, table/base deletion, or permission checks. Applied consistently to row, grouped, aggregate and view-source-scope SQL. Diagnostic instrumentation was removed. The compared first-page plans fell from 125ms/two source loops to 64ms/one loop; these are local observations, not a general benchmark. The complete three-suite regression passes 60 tests/516 assertions, including the >10k export in 2.96s. The full verifier also exposed missing phase registration for the new WorkflowRunDetailPanel DOM test; it is now assigned to the browser phase and passes there (six assertions), rather than being skipped in the server phase. Raw tsc passed after the preceding S14 changes; tsc after this SQL change is pending, scoped Biome/diff pass. Full verification must still be rerun after the remaining dispositions/fixes.

- Whole-run result: database-and-standard finished with 2,827 passed, two failures and one DOM-only skip (the dedicated browser phase was not reached because this phase failed). Regex regression is fixed without removing input regex support: write validation and validateObjectListSchemaChange already enforce retained input rows under the table schema lock; SQL does not substitute its regex dialect, and calculated regex columns remain rejected. Real field-update/read regression accepts a matching regex, rejects one invalidating stored values, and preserves typed reads. S14 now localizes oversized and other calculation errors rather than exposing codes, and finalization maps the locale-specific required message rather than comparing only English. Focused 18 unit tests/80 assertions and two DB finalization tests/44 assertions pass; scoped Biome passes. Initial tsc found an inferred string-array tuple in the new test; corrected with as const, rerun pending. Combined >10k export passes alone in 4.29s but failed its 5s SQL budget in the full suite; that performance finding remains open, not waived as flakiness.

- Current whole-suite run (full-recovery.log; isolated verifier directory grids-verification-s77Y1g) found two failures in database-and-standard: Combined CSV export above 10,000 rows exceeded the existing 5-second query budget; field-validation-object-list.test.ts rejects an input text regex that the existing supported-input test permits. The latter is caused by applying calculated-cell SQL regex restrictions to ordinary stored input columns. Both remain open; do not weaken the existing feature test or increase the query timeout merely to make the suite green. The verifier is still running at this checkpoint, and code is unchanged while it runs. S14 raw calculation codes are also confirmed still present; address after this run.

- T5 fixed in the real multi-format workflow regression: the renderer stub records request bodies, and assertions run outside the fetch callback after execution/replay. A renderer error wrapper can no longer swallow the assertion. Focused kernel-backed PDF/CSV/JSON/XML test passes (31 assertions; other tests intentionally filtered). Financial regressions pass as recorded below; raw Grids TypeScript and scoped Biome pass after adding the explicit SQL result type. No full-suite claim is made from these focused checks.

- Financial recovery evidence strengthened: 12 isolated DB tests / 121 assertions now include cancellation and republishing during rendering, with zero retained documents/claims and successful issuance by a new run. A receipt-completion fault injected after the document INSERT verifies transactional rollback of document, file bytes and claims, then successful retry of the same confirmed receipt. This is a database failure simulation, not a killed-process experiment. Acceptance 13 now has a real revised-workflow/revised-output duplicate-business rejection; acceptance 14 demonstrates that a deliberately changed businessId is distinct even when the other mapped values match. No production behavior was changed for these tests. Lease-specific kernel evidence and final whole-suite verification remain open.

- R4/R13 verification completed: the formula-reference tables used 7,107 bytes solely for source-column padding. Compacting those table spaces in both locales retains their content and raises measured registry headroom from 334 to 7,441 bytes (516,847/524,288); no platform limit or runtime contract changed. The Assistant pointer initially exceeded its existing 10,000-character budget; redundant prose was shortened instead of raising the cap. Final 20 Help/Skill tests pass (577 assertions), including every formula signature, GQL/formula examples, locale parity and both budgets. Earlier CLI/Help run: 97 tests, 1,342 assertions. Raw Grids TypeScript, scoped Biome and diff checks pass. R4 is resolved at the current corpus boundary; future additions still have to pass the existing registry check. No model-session evaluation or deployment was performed for the short Skill routing addition.

- Knowledge/CLI review pass: S11/S13/S15 corrected. CLI advertises --parameters-stdin, accepts the persisted record run channel, and uses format-neutral output paths for generation/download (actual PDF preview/merge paths retain PDF wording). CSV delimiter guidance moved into CSV; spreadsheet protection explicitly includes leading +/- phone numbers. The public app overview no longer claims every Document has a template/Record and describes dataSnapshot as summary metadata, not raw captured rows. R13 adds a short workflow Help routing pointer to the built-in Assistant Skill for query/data/output and financial confirmation, explicitly distinguishing generation from payment/import. This is source-only; no owned user Skill was overwritten. R12 disposition: retain explicit waiting-until-confirm-or-cancel semantics, not an arbitrary new expiry. Help/CLI now say there is no automatic expiry; authorization and source-version checks still run before issuance. Prepared captures/receipts are durable, bounded per run, and financial claims are only reserved at issuance. No new retention/deletion policy is invented.

- Remaining optional-review dispositions: S3 (catalog plan authorization caching) is not a correctness blocker; current code re-evaluates the published scope and a separate cache would need an explicitly scoped lifetime. Do not add it without measured need. S6's three small string-map comparisons do not justify a new shared abstraction in this remediation. S12 intentionally uses text workflow inputs for exact decimal digits; the existing CLI reference already instructs that model, and query binding validates the decimal type. Q4 is intentional: a published launch permission delegates the financial review to that launcher; inputFor rechecks canExecuteRun and every source table, and the issuance receipt pins the actor. Requiring a Base grant would break this delegated Custom App contract. Full IBANs are necessary for that review; masking is UI disclosure, not authorization. Q5's suspected anonymous workflow buttons are contradicted by current custom-app-published-page.ts: navigate actions remain public, workflow actions are skipped without accessActorUser, row actions are populated only for a signed-in actor, and submit actions have the same guard. Existing integration coverage must be included in final verification; no redundant UI gate added.

- Q6 GQL/capture boundary: terminal GQL field/formula projections, formula predicates, derived conditions and aggregate inputs now use one typed SQL error guard. Unhandled calculation errors return localized BAD_INPUT instead of becoming nulls silently omitted from totals. The guard wraps the complete expression, not its operands; IFERROR and unselected IF branches remain valid, as do legitimate empty values. Workflow captures use this same preview owner and now forward locale. A real joined-list regression checks direct JSON selection, a formula and grouped sum rejection; a capture regression checks BAD_INPUT, German recovery text and an explicit fallback. The new sequence also repeatedly exposed a hang in concurrent public-ID projection queries on one transaction connection: pg_stat_activity showed idle-in-transaction after the record ID query, with other connections idle. Ordering the three existing batched queries in toPublicGqlResponse removes that concurrent pipeline; the formerly hanging sequence passes. Final focused verification: 51 DB tests/522 assertions across six query/capture suites, 39 native compiler tests/100 assertions, 17 help tests/553 assertions, raw Grids tsc and scoped Biome pass. Help and CLI now list every LIST_* signature, explain the error boundary, and distinguish live draft formulas from captured final values; removed an older internal database-name leak from end-user help. Native aggregate guard received a final small follow-up after the DB run; broad final verification remains required. Scalar/regex disposition, Combined missing-capture diagnostics and the remaining epic items stay open.

- Q6 projection seam closed: formula, lookup/rollup and view-computed projections now select their compiled error predicate alongside the value. Record hydration preserves a typed null with a localized fieldErrors diagnostic, instead of dropping the predicate. The existing formula compiler remains responsible for IF/IFERROR branch semantics and frozen values; no dependency re-evaluation was added. The real record/snapshot regression checks invalid-list totals, IFERROR fallback, an unselected IF branch, public field IDs and snapshot rejection. A real SQL view-column regression checks the same metadata handoff. Verification: seven projection unit tests (14 assertions), 22 snapshot/list DB tests (213 assertions), then 36 list/finalization DB tests (403 assertions), raw Grids TypeScript and scoped Biome pass. Remaining GQL boundary investigation: sql-compiler-fields, grouping and where consume expression.sql without its error predicate; deciding the terminal query diagnostic contract is still open and this record fix does not claim to resolve it.

- Q6 SQL select/numeric progress: SelectConfigSchema is reused by the SQL owner. Select inputs now validate option membership, required/min/max selection counts and array element types; normalization drops empty IDs and duplicates while retaining first order. Numeric list inputs use the existing safe numeric parser plus explicit finite/type errors, avoiding raw cast exceptions for malformed retained cells. Eighteen DB parity tests pass (186 assertions), including removed options, null/non-string options, duplicate IDs, invalid numbers, NaN/Infinity and wrong JSON types. Next confirmed propagation gap: buildFormulaSqlProjections and buildComputedColumnSqlProjections discard compiled.expression.errorSql, and applyComputedProjections has no corresponding error column. Fix through that projection owner, preserving IF/IFERROR and frozen-value semantics, not by reparsing dependencies after SQL. Remaining date/boolean/text shape checks, regex disposition and public query/export validation remain open.

- Q6 SQL progress: list container/row shape and min/max cardinality now contribute to the existing formula error predicate without unsafe array expansion. Input scalar columns reuse normalizedCalculation checks, including tightened numeric range/scale and required constraints, instead of blindly projecting them. Finalized list snapshots still bypass current constraints and recalculation. The shared error predicate preserves SQL IF/IFERROR short-circuit behavior, verified with malformed containers. 35 list/finalization DB tests pass (339 assertions); the final 17 list tests pass (127 assertions) after the IF/IFERROR regression addition. Raw tsc and scoped Biome/diff checks pass before the final test-only extension. Remaining Q6: select-input constraints, malformed scalar shape/cast behavior, regex SQL support/disposition, public query/export error propagation and final consumer verification. Do not treat this checkpoint as full Q6 closure.

- Q6 follow-up evidence: final raw Grids tsc passes after narrowing a test diagnostic, and the final 25 relation tests pass (47 assertions). `compileObjectListRow` currently projects input columns without their scalar validation error predicates; `compileObjectListValue` handles only calculated-cell errors and does not guard array cardinality/shape. This is the next correctness seam: preserve existing IF/IFERROR evaluation semantics while making invalid list inputs explicit to SQL, GQL exports and finalization. The read repair change above is not a claim that SQL parity is resolved.

- Q6 read/repair seam implemented: invalid stored object lists retain their original value and carry a separate `fieldErrors` map. Public DTOs convert diagnostic keys to public Field IDs, and FieldValue displays the diagnostic. JS formulas and computed columns evaluate with errors in scratch values; no error string replaces the list. Corrected values clear their diagnostic. Document snapshot graph capture refuses invalid fields without persisting a snapshot. Focused tests: 25 relation units (47 assertions), 9 snapshot/SSR tests (51 assertions before a test-only type narrowing). SQL-projected calculations/exports and broader public consumers still require verification; Q6 is not closed. Do not override SQL-projected values using a naive dependency walk: IF/IFERROR short-circuit semantics must remain authoritative. No stored data or historical capture was changed.

- R3/Q2: SEPA confirmation previews now expose bounded advisory warnings for extended characters and past execution dates, localized at the HTTP boundary and shown with existing NoticeCards. Date comparison uses the request timezone. Captured input, names, dates and confirmation hashes are unchanged; warnings do not block confirmation. EPC guidance permits bank-agreed extensions, so neither silent transliteration nor blanket ASCII rejection is appropriate. Source: https://www.europeanpaymentscouncil.eu/sites/default/files/KB/files/EPC217-08%20Draft%20Best%20Practices%20SEPA%20Requirements%20for%20Character%20Set%20v1.1.pdf (basic set and customer-bank agreements). Seven profile/API tests pass (56 assertions), the dialog behavior test passes (10 assertions), and 44 isolated workflow action tests pass (351 assertions), including a past-date preview followed by unchanged-hash confirmation. Raw Grids tsc, scoped Biome and diff whitespace pass. EN/DE help and CLI reference are current; two help budget tests pass (16 assertions). Q3 DATEV target encoding remains unverified, not implicitly certified by these checks.

- Epic and five child tickets created; financial issuance remediation started.
- B1/B13: removed the pre-render claim reservation; the final issuance transaction now rechecks and locks source versions before reserving claims. Regression first reproduced stranded claims. Seven financial issuance tests pass, covering renderer retry, post-render permission loss, concurrent/repeated issuance, source changes during rendering, confirmation and duplicate export rejection. Existing pre-fix orphan cleanup and the remaining recovery matrix are still open.
- B14: runtime mutation middleware applies the credential write cap, including the two optionally authenticated form routes. Existing Custom App integration tests pass; coverage includes read-only credentials and retains normal form behavior.
- B7: relation label loaders carry finalization state, so presentation formulas use their captured values. The focused finalization/picker regression passes.
- B8: percent validation rounds decimal values before conversion to its bounded JSON number, matching PostgreSQL numeric ties. Eight scalar tests pass. Broader object-list SQL parity remains to verify.
- B10/B11: waiting run events reload authorized step details, and export confirmation has localized status text. Display tests pass. The DOM regression now proves a waiting event arriving during an in-flight detail read queues a second authorized reload and restores the Review export action from endpoint metadata; no confirmation is trusted from stream summaries. One behavior test passes (6 assertions), raw tsc passes.
- B12/S7: generation and capability download links use the primary artifact; generic downloads set nosniff and do not invent a PDF filename. A CSV generation route regression was added.
- B15: evidence metadata includes profile output. All six evidence integration tests pass with the suite's own Sync lifecycle. The initial focused runner incorrectly preloaded Sync and was corrected; that runner failure was not an application defect.
- B4/B17: workflow output paths in both help locales and the CLI reference now match the binder; English primary-download guidance matches German. Help size and binder tests pass.
- Q8: GQL scalar/list parameters reject NUL strings before PostgreSQL; focused parameter tests pass.
- Raw Grids TypeScript check passed during this batch. Full final verification has not run, and no child ticket is complete yet.
- B16: publication explicitly marks bound parameter samples as type-only. Select comparisons and membership defer only option membership for those parameters, not static literals, operators or types. Runtime binding remains strict. Sixty resolver/canonical/parameter unit tests and twenty query-capture/object-list SQL tests pass, including real publication-to-capture with option labels, IDs and rejected unknown values. The canonical stored source retains parameters, never sample metadata.
- B8: new object-list SQL regression confirms percentage decimal ties (1.075, 2.675, 0.125) match JavaScript and stay JSON numbers. Included in the twenty passing DB tests above.
- Follow-up raw Grids TypeScript check passed. Scoped Biome formatting/check and whitespace check passed. No existing database was modified: focused DB runs used an ephemeral local verification database.
- B2 implementation in progress: finalization now stores per-field required table IDs in finalized_computed_dependencies, captured in the same SELECT as computed inputs. Nested finalized targets contribute their historical dependencies, even after their formula becomes a constant. Unknown provenance is not treated as an empty dependency set. No backfill or historical-value rewrite was performed.
- The SQL frozen-value owner and stored-record get/list paths now use this metadata. A regression proves transitive values are null for a narrowed viewer, remain exact for an authorized viewer, and cannot be inferred through a formula predicate after a definition change. The new regression passes (13 assertions). The finalization plus record/query parity suites passed 21 tests before the final predicate extension; raw Grids TypeScript passed after the owner changes.
- B2 remains open: finish GQL authorization candidate discovery for historical dependencies (without widening explicit scopes), relation labels/expansions, record group/aggregate and Combined sources, legacy unknown-capture presentation, broader regressions, and documentation. The existing label batch now redacts using its supplied scope, but scope discovery must be completed so authorized historical labels are not unnecessarily lost. Do not treat the passing get/list regression as full closure.
- B2 follow-up: GQL and workflow capture use the actually authorized table scope rather than only current expression dependencies. Explicit Custom App scopes remain restrictive. Relation labels/expansions, record count/group predicates and formula preview now use captured authorization too. Formula preview also uses the existing record mapper so it does not recalculate finalized dependencies. The expanded historical-dependency test covers authorized and denied get/list/filter/labels/count/group/GQL/preview reads.
- Verification: 49 finalization/Combined/workflow-query tests passed; then 23 finalization/record-query-parity/formula-preview tests passed (233 assertions). All ran in disposable local databases. Scoped Biome passed. A requested nonexistent records-parity filename in the first invocation was corrected to records-query-parity.integration.test.ts in the second; it is not counted as first-run coverage.
- Combined source clarification: its mapped fields are explicitly published by a source Base administrator. Target readers intentionally need no source Base grant (grids-combined-tables.help.md). Keeping that publication contract is correct, not a B2 bypass. The Combined integration suite passed unchanged. Relation-picker HTTP callers separately require Base read or template write access; they are not narrowed Custom App readers.
- B3/B9 disposition: reject the proposed live-computation fallback for finalized rows. It would invent historical values and violate the frozen-value contract. Missing captures need explicit unavailable presentation/documentation, not recomputation. This work remains open.
- R9 source check: renderWorkflowDocumentsPdf already visits every authorized document and returns the explicit noCanonicalPdf error for a non-PDF primary artifact. It does not filter non-PDF documents out. Add focused mixed-format evidence before closing the risk; no speculative merge rewrite is needed.
- R9 verified: a real issued single-PDF run downloads unchanged; adding an issued CSV makes the run PDF download fail with BAD_INPUT instead of returning a partial PDF. The focused DB regression passes five assertions, without Gotenberg. Initial fixture omissions (run profile and correct kernel state) were corrected, not application constraints weakened. No production merge fix was necessary.
- B14 expanded regression now passes all twelve runtime mutation routes with read-only credentials. This includes comments and file changes. Finalization/preview raw TypeScript verification and whitespace checks passed in this batch.
- B6: stored object-list validation projects the current columns, while new writes still reject unknown keys. This enables removal on drafts without rewriting stored values, record versions or history. Finalized non-null column values still block removal through the existing schema guard. Fifteen object-list unit tests pass; the focused DB regression passes sixteen assertions, including unchanged underlying data/version, restoration of the original schema, and rejection after finalization. EN/DE help and CLI reference explain the distinction. This is read projection, not a data migration or a relaxation of public input validation.
- R14: the historical reset now locks and refuses retained document/run links and existing workflow profiles before dropping any schema. Removed its obsolete document-detachment UPDATE. Applied version remains unchanged, so normal restarts use the existing migration path. The new isolated regression proves both refusal branches preserve rows and do not mark the migration applied. All 27 migration integration tests pass (168 assertions), including repeated starts. Operator-facing Grids deployment documentation explains the preserving-migration requirement. No real installation or stored data was reset.
- B5: new query bindings carry schemaHashVersion 2. They omit column position and, without search, presentable and select-option/default presentation. Search retains those relevant definitions; other calculation/type config remains conservative. Unversioned existing bindings still verify using the exact original version-1 input, including its metadata; no stored hash or version is rewritten. Explicit republishing adopts v2. Runtime capture and dry-run planning propagate the version. Regression proves unchanged legacy bindings work, modern bindings survive reordering/option additions, and search or calculation changes still conflict. Thirteen focused query/action DB tests pass (163 assertions); seven query/capture-store tests passed before the final search assertion extension. Raw tsc and scoped Biome pass.
- B5 knowledge: CLI and EN/DE help explain republishing older bindings. The added help text initially crossed the existing registry budget; concise rewrites restored the passing catalog check without increasing the limit. R4 remains open: this is not adequate long-term headroom. R1 remains separate: canonical JSON collation and retained integrity hashes have not yet changed.

## Next correctness boundary

R8 final local check: raw Grids TypeScript passes after the test narrowing. All verification processes from this checkpoint have completed.

R8 implemented: source captures now share the existing 5-MiB input budget across a run, instead of resetting it for each step/loop iteration. The existing run profile has an additive nullable captured_bytes counter. Its transactional update serializes writers (and conflicts rather than overallocating under stale repeatable-read snapshots). New captures reserve canonical JSON bytes in the same transaction as their insertion/outcome; failed transactions roll the counter back. Existing steps are replayed before any new reservation. Retained pre-counter captures are conservatively counted from stored JSON without rewriting payloads/hashes. There is no separate storage model or process-local counter. Both production call sites already supply step transactions. EN/DE help and CLI describe the combined limit and recovery by smaller selections/separate runs.

Verification: 45 isolated capture-store/workflow-action tests pass, 380 assertions, including concurrent 3-MiB captures (only one fits), rollback, replay-neutral accounting, retained pre-counter rows and unchanged hashes. Two help-catalog tests pass (16 assertions). Initial raw tsc found only an unchecked test-array lookup; it is now explicitly narrowed and the rerun is pending. The broader epic remains open.

R8 investigation: `persistWorkflowQueryDataInTransaction` is the single durable capture write owner for query and value sources. It currently validates each capture and inserts idempotently by run_id/step_key but performs no cumulative accounting. A publisher-only forEach prohibition would not protect retained published plans, so it is insufficient. The required runtime fix must serialize allocation per run in the existing step transaction, count only new successful captures, leave replays and rollbacks budget-neutral, and use a documented operational budget. Avoid process-local counters or a second capture store. Existing per-capture 5-MiB and loop limits make the risk bounded but potentially multi-GiB, not literally unbounded. No runtime budget has been implemented at this checkpoint; do not close R8.

R7 disposition: retain deterministic DSL short IDs in fixtures. The supported Grids verifier creates a unique disposable database for each invocation and runs test phases with max-concurrency 1 (`scripts/verify.ts`); a crashed invocation cannot collide with a later invocation's database. The completed full checkpoint used this runner. Running raw integration tests against a shared application database is not the supported acceptance path. Rewriting roughly 90 syntax examples adds no production protection at this seam. This does not claim that arbitrary parallel execution in a shared database is supported.

Raw Grids TypeScript passed after the final capture-query changes as well. No verification process is left running at this checkpoint.

Final check for the capture-query guard: 42 isolated DB tests pass across finalization, record-query parity and GQL aggregates (474 assertions, no failures). This includes ordinary list readability with absent captures. Scoped Biome and git diff --check pass. The earlier aggregate filename typo meant the prior run covered only the two explicitly found suites; the corrected three-suite run above is the acceptance checkpoint. Remaining boundaries below are still open.

B3/B9 query completeness checkpoint: the frozen SQL owner can require a captured type and value key, without mistaking a captured JSON null for a missing capture. GQL projections and native record predicates/groups/aggregates enable that guard; normal record display remains readable. Missing values produce bounded BAD_INPUT with EN/DE recovery guidance, not a partial sum/count. A new DB regression also exposed a separate bug: aggregate/group GQL omitted the resolved record metadata filter. The shared aggregate extra-WHERE owner now applies it, so draft-only calculations actually exclude finalized records. The last completed 23-test finalization/parity run passed 254 assertions; the final strengthened guard and aggregate suite rerun is pending. Raw tsc passed before the last presence-key strengthening.

This does not close B3/B9: Combined mapped source calculations, row-level unavailable diagnostics, and invalid object-list diagnostics still need completion. An experimental projection narrowing was removed before acceptance: the existing plan dependency collector is used by stored schema hashes and does not yet prove complete transitive formula-name discovery. Do not use that collector to omit Combined source data without verifying dependencies and retained hash versions. The current Combined source contract was preserved. No data backfill, historical-value recomputation, commit, or deployment occurred.

B12 source/profile disposition: do not restrict ordinary templates to PDF media types. Built-in CSV, JSON and XML profiles accept typed profile input from a record template and use the same immutable issuance owner. Three new DB tests create real templates, issue their built-in profiles, check exact decimal bytes and primary metadata, and replay without duplication (18 assertions). Nested XML Liquid is explicitly quoted with the existing raw tag in the outer profile-input template; the initial unquoted fixture correctly failed rather than silently losing rows. The financial-profile test now also checks template-write rejection, not just registry and preview absence. The existing generate-route test confirms primary-artifact download metadata. Financial DATEV/SEPA profiles remain restricted to confirmed financial issuance; no new media-type gate or second template registry is needed.

Q7/B18 scenario 8: retain nonempty financial batches and source-version guards. Empty automatic source selection now returns a distinct EN/DE BAD_INPUT telling the user to check selection and filters, instead of suggesting explicit versions for an empty set. New DATEV and SEPA tests check both schema and real renderer refusal of empty rows. Twelve profile/source-version tests pass (106 assertions); the earlier combined render-route/profile run passed 75 tests (363 assertions). Raw Grids TypeScript and diff checks pass after the final edits. This closes the missing empty-batch unit evidence, not the still-open claim/kernel acceptance scenarios. No external bank or DATEV interoperability claim follows from these tests.

Q10 verified without another runtime guard: a real finalization test proves that deleting an input used by a live formula returns 409 and leaves its frozen result intact. Deleting the formula itself, then its input, is supported and retains both typed values and field definitions in the immutable revision. One isolated DB test passes (10 assertions). The existing dependency owner, not a second finalization deletion model, enforces this boundary.

S2 implementation: parameter validation still precedes reads; references resolve in one deduplicated batch, with table authorization once per table and identity-only SQL through the record-read owner. Missing, deleted and wrong-table references are not projected. Planned records still require table access but never reach SQL. Combined sources reuse their publication guard. The new integration test exposed that Combined SQL omitted short_id; the existing source now carries it, preserving public identity without recalculating records. Seven parameter tests pass (43 assertions); all 29 Combined DB tests pass (200 assertions), including the new identity boundary, and all 44 workflow-action DB tests pass (351 assertions). Raw Grids TypeScript, scoped Biome and diff whitespace checks pass. S3 and the remaining items stay open.

B3/B9/R15 knowledge: EN/DE table help and CLI reference now explain that absent historical calculations are not recomputed or zero, including fields added later. Four-eyes explicitly covers every live field definition/name, not only totals. Existing history/file lifecycle paragraphs were shortened without dropping retention, authorization or atomicity boundaries. The initial help addition exceeded the registry cap; the revised corpus and reference tests pass (4 tests,37 assertions). B3/B9 runtime diagnostics and protection against silent incomplete aggregates remain open; this documentation is not presented as their fix. R4 durable headroom remains open.

R1 remaining digest owners now versioned: new schema bindings use v3 (v2 structural dependencies with canonical JSON v2); v1 and v2 bindings retain their previous algorithms. Issuance receipts and issued document metadata carry immutable hash_version, with additive defaults of 1 for retained rows and explicit 2 for new writes. Request identity, confirmation, normalized financial data, run binding, template and profile hashes use the receipt's algorithm; replay never silently substitutes a new hash. Evidence metadata exposes the document hash_version. A raw legacy financial receipt regression covers inspect, confirm, completion and replay, retaining its original confirmation hash. New/old document hashes are checked against their recorded algorithms. Verification: 43 issuance/financial/migration DB tests pass (391 assertions), six evidence tests pass (124 assertions), two canonical JSON units pass, raw Grids tsc passes. Earlier query/financial regression run passed 14 tests (135 assertions), including retained schema v1/v2 and new v3. No stored payload, confirmation or hash was rewritten. Old v1 hashes remain dependent on their original environment; no unknown historical locale is guessed and integrity failures remain fail-closed. Full final verification and the other review items remain open.

R1 first storage seam: canonical JSON now supports explicit version 2 (UTF-16 key ordering, no locale collation or Unicode normalization). Workflow captures use v2 and persist hash_version; additive migration defaults existing rows to v1 without rewriting payloads or digests. Reads select the stored algorithm, never try an unchecked fallback. Evidence query entries expose sourceHashVersion alongside the historical sourceSha256. Two Unicode/version unit tests, 14 capture/source/financial DB tests (125 assertions), and all six evidence integration tests (123 assertions) pass. The evidence fixture intentionally represents v1; its initial v2 expectation was corrected to verify the retained version rather than changing its history. Raw tsc passed before the evidence metadata addition; final verification remains. R1 is NOT complete: issuance receipts, profile hashes and schema bindings still use their existing v1 contract until separately versioned.

R15 disposition: keep the conservative approval snapshot of every live field's ID, name, type, config and required flag. Four-eyes approves the complete record under its displayed meaning, not just a numeric computation; renaming an otherwise unchanged field can change that meaning. Requesters must submit a new review after such a schema change. Narrowing this to computed dependencies would weaken the reviewed-record contract for convenience. Source evidence: `calculationApprovalSnapshot` and both snapshot equality gates in `record-finalization.ts`. This is intentional stricter behavior, not a missing authorization fix. User-facing clarification belongs with the remaining finalization knowledge pass. Cosmetic late-import cleanup (S9) and speculative lateral-SQL optimization (S10) are not correctness prerequisites; preserve them unless measured evidence justifies a separate change.

R5/R10/R11: the financial dialog reuses the existing exact decimal formatter for totals and row amounts; preview HTTP metadata carries the request-derived timeZone and the UI uses the document date formatter. Credential-bound confirmation is retained deliberately: a different token/session is not the initiating authority. Actor mismatch and stale-run errors now have distinct EN/DE recovery text. Preview and planning source-version checks run read-only without row locks; confirmation/issuance retain the locking default and recheck everything. The preview regression observes PostgreSQL read-only mode, which also forbids accidental locking reads. All 51 financial/workflow action tests pass (424 assertions); the dialog behavior test passes (9 assertions); raw tsc passes. One intermediate API equality assertion needed the new display-only timeZone metadata; the full focused rerun is green. CLI guidance describes the credential boundary and timezone metadata. R12 expiry/retention and the kernel recovery matrix remain open.

Full verification checkpoint (2026-09-12): all eight Grids verifier phases pass without skips, 2,888 tests and 20,715 assertions. This includes isolated DB/standard tests, outbox, Sync, evidence, browser bundle, real Gotenberg PDF, recovery/cleanup and 59 DOM behavior tests. Log directory: `/var/folders/pj/92xppstn3r1b739_l8dcl0_40000gn/T/grids-verification-9XWBoE`; aggregate log `/tmp/grids-remediation-j2sL6t/full-current2.log`. Raw Grids TypeScript and `git diff --check` pass. This verifies the current remediation checkpoint, not the remaining R1, historical-capture UX or other undisposed review findings; the epic remains active.

Verification checkpoint: the first complete run reached 2,801 passing tests but failed two outdated projection fixtures and a newly added parameter test against an already-loaded pre-edit module. Do not treat this mixed-state run as acceptance. The table-policy test now uses real Base/table rows; the relation-search fixture selects only its relevant relation, with an added negative assertion that unreadable rollups remain rejected. All 25 focused projection tests pass (232 assertions). Parameter resolution checks the projected six-character-ID budget before record lookups and resolves repeated identities once; six unit tests pass (37 assertions). Raw Grids tsc passes again. A fresh full verifier is running against the unchanged source checkpoint.

R2/Q8: shared parameter schemas now enforce the existing 100-name, 10,000-list-entry and 20,000-character JSON budgets across HTTP, direct binding, workflow resolution and capabilities, including NUL rejection. Preview and execute capabilities bind optional parameters through the HTTP context converter. Nonempty parameter maps deliberately omit editor links: the editor has no runtime-value URL contract, and values must not be silently dropped or leaked into URLs. Thirty-two focused unit tests pass (155 assertions); the real DB capability regression passes (145 assertions), including both operations. Initial tsc found a test-only inferred optional-property union; it was replaced with the declared parameter-map type. Final tsc and broader verifier results remain to record.

B2/B3/B9 are not resolved by the above changes. Frozen values must retain historical dependency authorization even if a formula or a relation target definition changes later. A local null assignment for currently hidden lookups does not cover derived formulas or changed historical dependencies. Investigate one capture/read owner rather than adding independent redaction rules in APIs. Do not fabricate old values or automatically recompute old finalized rows. Any schema-hash change also needs an explicit disposition for already published bindings; do not silently bypass mismatch checks.

Source clarification: ordinary grants are Base-wide, but published Custom App readers explicitly narrow readableTableIds and seed tableReadAccess. Therefore the frozen-value issue is still a real boundary defect, not evidence of ordinary per-table grants. Historical dependency metadata must cover nested captured targets and derived local formulas; using only the current field definitions is insufficient. Unknown old capture provenance must fail closed in narrowed readers, never be reconstructed as historical truth from current values. New provenance and values must be captured from one database snapshot. Both SQL predicates and raw record-data reads must enforce the same rule.
