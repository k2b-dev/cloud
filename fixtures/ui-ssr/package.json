{
  "name": "@k2b/ui-ssr-fixture",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run --cwd ../../packages/ui build && NODE_ENV=production bun run scripts/build.ts",
    "dev": "NODE_ENV=development bun --preload ./src/preload.ts ./src/server.ts",
    "test": "bun run --cwd ../../packages/ui build && bun test",
    "typecheck": "tsc -p tsconfig.json --noEmit --pretty false"
  },
  "dependencies": {
    "@k2b/ssr": "catalog:",
    "@k2b/ui": "workspace:*",
    "hono": "catalog:",
    "solid-js": "catalog:"
  },
  "devDependencies": {
    "@types/bun": "catalog:",
    "typescript": "catalog:"
  }
}
