# NATS configuration for CI jobs. Mirrors the development server: max_payload
# must cover the largest @k2b/sync publish (Notebooks Yjs full-state resync
# is about 8 MB); Sync's ready() refuses smaller servers.
max_payload: 16MB
jetstream {
  store_dir: /data
}
