{
  "name": "@k2b/cloud-app-spaces",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "module": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./cli": "./src/cli.ts",
    "./capability-contracts": "./src/capability-contracts.ts",
    "./integration": "./src/integration.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit --pretty false 2>&1 | grep -v node_modules | (! grep -q 'error TS')",
    "test": "bun test",
    "test:dom": "bun test --isolate --conditions=browser --preload ../ui/test/solid-dom-preload.ts ./test"
  },
  "dependencies": {
    "@k2b/ssr": "catalog:",
    "@k2b/stdlib": "catalog:",
    "@k2b/sync": "catalog:",
    "@k2b/ui": "workspace:*",
    "@k2b/cloud": "workspace:*",
    "dayjs": "catalog:",
    "hono": "catalog:",
    "hono-openapi": "catalog:",
    "ical-generator": "10.2.0",
    "solid-js": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@nats-io/transport-node": "catalog:",
    "@types/bun": "catalog:",
    "typescript": "catalog:"
  },
  "license": "AGPL-3.0-or-later"
}
